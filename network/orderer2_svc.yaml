apiVersion: v1
kind: Service
metadata:
  name: ord2-hlf-ord
  namespace: orderers
  labels:
    run: ord2-hlf-ord
spec:
  type: ClusterIP
  selector:
    name: ord2-hlf-ord
  ports:
  - protocol: TCP
    port: 7050
    name: grpcs