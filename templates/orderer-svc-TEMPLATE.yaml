apiVersion: v1
kind: Service
metadata:
  name: ordORD_NUMBER-hlf-ord
  namespace: orderers
  labels:
    run: ordORD_NUMBER-hlf-ord
spec:
  type: ClusterIP
  selector:
    name: ordORD_NUMBER-hlf-ord
  ports:
  - protocol: TCP
    port: 7050
    name: grpcs